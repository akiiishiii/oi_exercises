#include <iostream>
#include <set>

int const Maxn = 100005;

struct segment_tree {

} tree[Maxn << 2];

int main(int argc, char const *argv[]) {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    return 0;
}