// 1075.cpp
#include <cmath>
#include <iostream>

int main(int argc, char const *argv[]) {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    double h, s1, v, l, k;
    int n;
    std::cin >> h >> s1 >> v >> l >> k >> n;
    double t = sqrt(2 * (h - k) / 10);

    return 0;
}