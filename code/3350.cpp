// 3350.cpp
#include <iostream>
#include <algorithm>
#include <functional>

int const Maxn = 50001;
int n, m, pos[Maxn], c[Maxn];
long long s[Maxn], ans;
struct dat {
    int l, r, id;
    long long a, b;
} ds[Maxn];
struct comp


int main(int argc, char const *argv[]) {

    return 0;
}
