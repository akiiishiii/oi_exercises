// 1539.cpp
#include <iostream>
#include <queue>

int main(int argc, char const * argv[]) {
    int n, k;
    std::queue<long> q1, q2;
    std::cin >> n >> k;
    for (int i = 0; i < n; i++) {
        long temp;
        std::cin >> temp;
        q1.push(temp);
    }
    while (true) {

    }
    return 0;
}
