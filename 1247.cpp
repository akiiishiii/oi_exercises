// 1247.cpp
#include <iostream>
#include <string>
#include <stack>

std::string st;
std::stack<double> s;

double getnum(int i);
double calc();

int main(int argc, const char * argv[]) {
    std::cin >> st;
    for (std::string::iterator it = st.begin(); it != st.end(); it++) {

    }

    return 0;
}