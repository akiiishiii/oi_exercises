// 1256.cpp
#include <iostream>

template <typename T>
inline T max(const T a, const T b) { return a < b ? b : a;}

int main(int argc, const char * argv[]) {
    int n, d, k;
    std::cin >> n >> d >> k;

    return 0;
}
